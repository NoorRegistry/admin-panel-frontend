const endpoints = {
  authentication: {
    login: "/v1/api/admin/auth/login",
    refreshToken: "/v1/api/admin/auth/refresh",
  },
  stores: {
    index: "/v1/api/stores",
  },
};

export default endpoints;
