export interface IShowProductInfoDrawerConfig {
  productId?: string;
  open: boolean;
}

export interface IShowCategoryInfoDrawerConfig {
  categoryId?: string;
  open: boolean;
}

export interface IShowRelatedProductsDrawerConfig {
  productId?: string;
  productName?: string;
  open: boolean;
}
